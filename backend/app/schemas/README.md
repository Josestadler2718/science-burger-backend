# schemas/

Placeholder folder. Will hold Pydantic schemas used for request/
response validation in the FastAPI routes (e.g. `ProductOut`,
`OrderCreate`, `ContactMessageCreate`).

Not implemented yet — this stage of the project is front-end only.
